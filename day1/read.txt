HI shila 
this is your first day project strat from git 1 day
I create new project and make 1 folder day 1 write some code instuction then push to github
1. create repo first in github for track file code 
2. go to your vs code then open terminal and write --> git init (git init code for connection git initialized empty file.)
3. git status then we can see visible ealrlier careted file means i creted txt file.
4. Git add . meand all files or we can write git add file name and enter
5. again check file status what is green or red if wroking fine then visible --> new file: day1/read.txt
6. add commit for push code like git commit -m "status"
7. git push on your github repo file.
8. if you want to change repo path then 
--> git remote remove origin <path url> remove old path 
--> git remote add origin <path url>  for new repo file connect to project
--> git remote -v check status 
first check bydefault master branch is active to change main branch then push code
--> git branch 
--> git branch -M main (master to main)
--> git branch (status) 
--> git push -u origin main(push your repo folder in main branch ) 
if change again your vs code means your file is midified: for check command -->git status
if any wrong content added and commit code how to restore then just git restore commad enter --> git restore --worktree then remove wrong code.
if you want wrong content delete process - 
comment + content delete then apply --> git reset --hard HEAD~1 
comment delete and code store in staging -->git reset --soft HEAD~1
git log -p -2 means only visible last 2 log
git log --stat means obly visible comment of logs
git log --pretty=oneline means only visible commitline